= simple_form_for @transaction do |f|
  = f.input :amount
  = f.input :date, as: :date, order: [:day, :month, :year]
  = f.input :comment
  = f.submit t('main_page_user.form_add'), class: 'btn btn-success'
