machine:
  timezone:
    Asia/Yekaterinburg

  ruby:
    version:
      2.4.3

dependencies:
  pre:
    - gem install bundler --pre

test:
  post:
    - bundle exec rubocop
    - bundle exec reek .
    - bundle exec rspec
