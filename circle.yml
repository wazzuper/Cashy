machine:
  timezone:
    Asia/Yekaterinburg

  ruby:
    version:
      2.4.3

test:
  post:
    - bundle exec rubocop
    - reek app -c reek.yml
    - reek config -c reek.yml
    - bundle exec rspec
