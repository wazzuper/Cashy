machine:
  timezone:
    Asia/Yekaterinburg

  ruby:
    version:
      2.4.3

test:
  post:
    - bundle exec rubocop
    - reek app -c config.reek
    - reek config -c config.reek
    - bundle exec rspec
